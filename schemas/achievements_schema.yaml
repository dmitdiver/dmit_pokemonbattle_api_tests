type: object

required:
  - data

additionalProperties: false

properties:
  data:
    type: array
    items:
      type: object

      required:
        - slug

      additionalProperties: false

      properties:
        slug:
          type: string

        is_reached:
          type: boolean